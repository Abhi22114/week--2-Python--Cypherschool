#List Vs Array


#c,c++,java (array can store one data types)
#list-can store many datatypes 
#list is very flexible

#Araay-can store one and only data-types (c,c++,java)
# javascript array is flexible =same as python list(list in python are flexible)

#python array module can store a fix data-types  
#numpy arrays(data science),numpy array --->>>>>>(c language)
# python ---->>>>binding with c languages libraries 