#tuple data structure
# tuple can store  any data type 
# tuples are immutable ,once tuple is created we cannot update
#tuples are faster than list
example=('one','two','three')
 #no append ,no extend ,no pop,no remove ,no insert method perform in tuples

#method in tuple
# count ,index
# len function
# slicing
print(example[0])

# tuple dosen't support item assingment